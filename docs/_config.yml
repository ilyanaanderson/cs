title: Ilyana Anderson
description: >- # this means to ignore newlines
  Welcome to the personal website of Ilyana Anderson.
  Here, she presents her Computer Science projects.
# in order to use ">-", do not use quotation marks around the string 
# otherwise, quotes are optional
# newlines are converted to spaces: remove any spaces at the end of lines

# baseurl: "" # empty string is likely the default
# url: "" # if commented out, it would be the default (local for local and the site for remote)
url: "https://ilyanaanderson.github.io"
baseurl: "/cs"


theme: minima
# plugins:
#   - jekyll-feed    
# jekyll-feed is active by default 
# will require title and description set in _config (or else it will look for them in github metadata)

